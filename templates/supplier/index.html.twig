{% extends 'admin.html.twig' %}

{% block title %}Suppliers{% endblock %}

{% block body %}
<div class="bg-white rounded-lg shadow p-6">
  <div class="flex flex-wrap items-center justify-between gap-3 mb-6">
    <div>
      <h1 class="text-xl font-semibold text-gray-900">Suppliers</h1>
      <p class="text-sm text-gray-500">
        {{ total }} supplier{{ total == 1 ? '' : 's' }} in system.
      </p>
    </div>

    <div class="flex flex-wrap items-center gap-3">
      <form method="get" class="flex items-center gap-2">
        <input
          type="text"
          name="q"
          value="{{ q }}"
          placeholder="Search by name or email"
          class="input input-bordered input-sm w-48 md:w-64"
        >
        <button class="btn btn-sm btn-primary" type="submit">Search</button>
        {% if q %}
          <a href="{{ path('app_supplier_index') }}" class="btn btn-sm btn-ghost">Clear</a>
        {% endif %}
      </form>

      <a href="{{ path('app_supplier_new') }}" class="btn btn-primary btn-sm">Add Supplier</a>
    </div>
  </div>

  <div class="overflow-x-auto">
    <table class="min-w-full divide-y divide-gray-200">
      <thead class="bg-gray-50">
        <tr>
          <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase">ID</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase">Name</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase">Email</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase">Phone</th>
          <th class="px-6 py-3 text-left text-xs font-semibold text-gray-500 uppercase">Created</th>
          <th class="px-6 py-3 text-right text-xs font-semibold text-gray-500 uppercase">Actions</th>
        </tr>
      </thead>
      <tbody class="bg-white divide-y divide-gray-100">
        {% for s in suppliers %}
          <tr class="hover:bg-gray-50">
            <td class="px-6 py-4 text-sm text-gray-500">{{ s.id }}</td>
            <td class="px-6 py-4 text-sm text-gray-900">{{ s.name }}</td>
            <td class="px-6 py-4 text-sm text-gray-900">{{ s.email ?? '—' }}</td>
            <td class="px-6 py-4 text-sm text-gray-900">{{ s.phone ?? '—' }}</td>
            <td class="px-6 py-4 text-sm text-gray-500">
              {{ s.createdAt ? s.createdAt|date('m/d/Y') : '—' }}
            </td>
            <td class="px-6 py-4 text-sm text-right">
              <div class="inline-flex gap-2">
                <a href="{{ path('app_supplier_edit', {id: s.id}) }}" class="btn btn-sm btn-outline">Edit</a>
                <form method="post" action="{{ path('app_supplier_delete', {id: s.id}) }}"
                      onsubmit="return confirm('Delete this supplier?');">
                  <input type="hidden" name="_token" value="{{ csrf_token('del_sup_' ~ s.id) }}">
                  <button type="submit" class="btn btn-sm btn-error btn-outline">Delete</button>
                </form>
              </div>
            </td>
          </tr>
        {% else %}
          <tr>
            <td colspan="6" class="px-6 py-4 text-sm text-gray-500">No suppliers.</td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>

  {% if pageCount > 1 %}
    {% set route = app.request.attributes.get('_route') %}
    {% set query = app.request.query.all %}
    <div class="mt-4 flex items-center justify-between text-sm text-gray-700">
      <div>Page {{ page }} / {{ pageCount }} ({{ total }} suppliers)</div>
      <div class="join">
        <a class="btn btn-sm join-item {{ page <= 1 ? 'btn-disabled' }}"
           href="{{ page <= 1 ? '#' : path(route, query|merge({'page': page - 1})) }}">
          « Prev
        </a>
        <a class="btn btn-sm join-item {{ page >= pageCount ? 'btn-disabled' }}"
           href="{{ page >= pageCount ? '#' : path(route, query|merge({'page': page + 1})) }}">
          Next »
        </a>
      </div>
    </div>
  {% endif %}
</div>
{% endblock %}
